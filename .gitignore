# Linked dev paths under src/ — not source files.
# src/assets   = directory junction → ../assets
# src/script.js = hard link        → ../script.js
src/assets
src/script.js

# Compiled CSS — build output of `sass src/index.scss`
src/styles.css
src/styles.css.map

# Generated — npm dependencies (restore with `npm install`)
node_modules/

# OS / editor cruft
.DS_Store
Thumbs.db
*.log
.idea
